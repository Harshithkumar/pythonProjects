sentence = "small residency caafe salvia koko is a"
wordsplit = []
wordsplit = sentence.split()
large = small = wordsplit[0]
for i in range(len(wordsplit)):
    if len(large) < len(wordsplit[i]):
        large = wordsplit[i]
    if len(large) > len(wordsplit[i]):
        small = wordsplit[i]

print("Smallest word is: ", small, "=", len(small))
print("Largest word is: ", large)

